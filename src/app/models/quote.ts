// TODO: Finish implementing the correct interface.
export interface Quote {
  id: number;
  content: string;
  author: string;
}

export interface SuggestedQuote {
  text: string,
  author: string
}
